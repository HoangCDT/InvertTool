version: '3.8'

services:
  web:
    build: .
    container_name: vision-invert-web
    ports:
      - "8000:8000"
    volumes:
      - ./static:/app/static
    environment:
      - ENV=production
    restart: unless-stopped
